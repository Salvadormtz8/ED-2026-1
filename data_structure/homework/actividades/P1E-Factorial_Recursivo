#include <stdio.h>
factRecursion(n){
	if (n < 0)
		return 0;
	else if(n == 0)
		return 1;
	else 
		return n * factRecursion(n-1);
}

int main(){
	int n, f;
	printf("Dame un numero: ");
	scanf("%d", &n);
	f= factRecursion(n);
	printf("El resultdado es: %d",f);
	return 0;
	
}
